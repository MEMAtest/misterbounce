import type { Video } from "@/types";

// Videos - add YouTube video IDs here when available
export const videos: Video[] = [];

// Featured video for hero section or highlights
export const featuredVideo: Video | undefined = videos[0];
